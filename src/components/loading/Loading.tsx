import React from 'react';
import { Text } from 'react-native';
import { loadingStyles } from './Loading.styles';

const Loading = () => {
  return <Text style={loadingStyles.container}>Loading</Text>;
};

export default Loading;
